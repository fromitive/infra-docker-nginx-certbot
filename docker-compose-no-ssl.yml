version: '3'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    network_mode: "host"
    volumes:
      - ./data/nginx-no-ssl/conf.d:/etc/nginx/conf.d
      - ./data/certbot/www:/var/lib/letsencrypt
